import React from 'react';
import FormLogin from '../components/FormLogin';

const PageConnexion = () => {
    return (
        <main>
            <FormLogin />
        </main>
    );
};

export default PageConnexion;